{
  "name": "infinibay-setup-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Infinibay first-run bootstrap TUI (Phase A). Collects pre-boot config and writes .env.docker before ./dev.sh brings the stack up.",
  "bin": {
    "infinibay-setup": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "pg": "^8.11.5"
  }
}
