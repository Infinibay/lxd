# LXD Compose Main Configuration
# https://mottainaici.github.io/lxd-compose-docs/

general:
  debug: false

logging:
  level: "info"
  runtime_cmds_output: true

# Define the directories list where load environments
env_dirs:
  - ./envs
