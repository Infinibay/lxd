# Environment variables and values (contains passwords)
.env
.env.docker
values.yml
TESTING.md

# Docker dev path: repos are cloned here and bind-mounted (not committed)
/repos/

# Temporary files
*.tmp
*.log
*.backup
profiles/generated/


# ken — local index, daemon socket, logs
.ken/

# local multi-node KVM test harness (copies dev secrets — never commit)
.env.livetest

# `./dev.sh join` compute-node runtime config
.env.node

# iby: identity test-engine assets materialised from the package on first `engine … up`
/engines/
