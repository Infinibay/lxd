name: infinibay-postgres
description: Infinibay PostgreSQL profile with persistent data
config: {}
devices:
  postgres-data:
    type: disk
    source: {{INFINIBAY_DIR}}/data/postgres
    path: /data
